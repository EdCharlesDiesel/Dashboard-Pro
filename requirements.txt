# pandas/numpy/scipy/statsmodels/arch pinned to the stable pre-3.0/2.x combo
# (not the latest available) — see runtime.txt: Streamlit Community Cloud's
# container segfaulted on Python 3.13 + pandas 3.0.2 + numpy 2.4.4 (a native
# C-extension crash, not a Python exception). This is the combo the quant
# ecosystem (scipy/statsmodels/arch) is actually tested against.
plotly==6.7.0
streamlit-autorefresh==1.0.1
pandas==2.2.3
numpy==1.26.4
yfinance==1.3.0
ta==0.11.0
fredapi==0.5.2
matplotlib==3.10.9
requests==2.33.1
tenacity==9.1.4
streamlit==1.58.0
scipy==1.13.1
pytz==2026.2
psycopg2-binary==2.9.12
pandas_datareader==0.10.0
statsmodels==0.14.2
arch==7.2.0  # GARCH vol forecasting (src/core/quant_models.py); optional, lazy-imported
cot_reports==0.1.3  # CFTC Commitment of Traders reports (src/services/cot_fetcher.py)
# data_backbone pipeline (src/data_backbone): SQLAlchemy store, APScheduler
# worker. psycopg2-binary is already pinned above.
SQLAlchemy>=2.0
APScheduler>=3.10
lxml==6.1.1  # HTML parsing backend for pandas.read_html (MT4 statement import)
# anthropic: optional — powers AI report summaries (src/core/ai_reports.py),
# which self-disable when it's absent. Not installed in the working venv, so
# left unpinned; pin once installed.
anthropic
reportlab==5.0.0  # docs/generate_guide_pdf.py only (builds assets/Dashboard_Pro_System_Guide.pdf); not imported by the running app
dukascopy-python==4.0.1  # src/data/dukascopy_backfill.py — historical archive for local debug mode's replay/time-machine (see src/debug/); not imported by the running app in prod

